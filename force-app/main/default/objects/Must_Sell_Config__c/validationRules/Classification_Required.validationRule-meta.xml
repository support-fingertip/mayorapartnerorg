<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Classification_Required</fullName>
    <active>true</active>
    <description>Classification is required when the config is active.</description>
    <errorConditionFormula>AND(
    Is_Active__c,
    ISBLANK(TEXT(Classification__c))
)</errorConditionFormula>
    <errorDisplayField>Classification__c</errorDisplayField>
    <errorMessage>Classification (Must Sell / Focused Sell) is required for active configurations.</errorMessage>
</ValidationRule>
